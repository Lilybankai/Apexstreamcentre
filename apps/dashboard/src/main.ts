// Dashboard shell placeholder. Real account/marketplace/multistream UI arrives
// in P2/P3; this confirms the app builds and is wired into the workspace.
const app = document.getElementById('app');
if (app) {
  app.innerHTML = '<p style="color:#7a8194">Build OK · ApexStreamCentre dashboard scaffold.</p>';
}
